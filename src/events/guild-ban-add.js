module.exports = (guild, user) => {
  member.send(`Hi ${user.username}, was banned by !`);

  // client.channels
  //   .get(helpers.getChannelId('logs'))
  //   .send(`${user.username}, was banned by .`);
  // });
};
